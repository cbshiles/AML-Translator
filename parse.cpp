#include <string>

#include "io.hpp"
#include "interpreter.hpp"

int main(int argc, char *argv[]){

  // std::string aml = readF(argv[1]);
  // std::string html = interpret(aml);
  // writeF(argv[2], html);
  
  return 0;
}
